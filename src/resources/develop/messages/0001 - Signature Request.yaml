#
# Signature Request: Partially-signed transactions (Tokenized actions, Bitcoin, Multisig, Threshold Signatures, etc.) can be passed around on-chain to the parties that still have to sign the transaction.
#

code: '0001'

metadata:
  label: Signature Request
  description: "Partially-signed transactions (Tokenized actions, Bitcoin, Multisig, Threshold Signatures, etc.) can be passed around on-chain to the parties that still have to sign the transaction.

fields:
  - name: SigRequest
    label: Signature Request
    description: "A Bitcoin transaction with multiple inputs from different wallets/users."
    type: nvarchar32
    example_value: ""
    notes: ""
