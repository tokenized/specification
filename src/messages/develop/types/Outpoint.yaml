#
# Outpoint provides a reference to a specific output of a bitcoin transaction.
#

code: 0002
name: Outpoint
label: Outpoint
description: "Reference to a bitcoin transaction output."

fields:
  - name: TxId
    label: TxId
    type: TxId

  - name: OutputIndex
    label: Output Index
    description: The index of the output within the referenced transaction.
    type: uint
    size: 4
