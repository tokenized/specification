
code: 1005
name: Distribution
label: Distribution
description: >
  An administrator is making a distribution/payout. Used to publicly declare these payments and
  allow holders to be notified of them via the smart contract agent.

metadata:
  outputs:
    - name: AssetContract
      label: Contract Public Address for Asset X
      comments:

fields:
  - name: AssetCode
    label: Asset Code
    description: >
      A unique code that is used to identify the asset. It is generated by hashing the contract
      public key hash and the asset index. SHA256(contract PKH + asset index)
    type: AssetCode
    notes: "Cannot be changed by the administration, operator or smart contract."
    required: true

  - name: Timestamp
    label: Timestamp
    description: "Timestamp in nanoseconds of when the administrator initiated the distribution."
    type: Timestamp
