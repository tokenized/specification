#
# Addition Action: Adds an entry to the Register.
#

code: R2

metadata:
  name: Addition
  label: Addition Action
  description: "Addition Action -  Adds an entry to the Register."

rules:
  fee:
  inputs:
    - name: Register
      label: "Register's Public Address"
      comments:

  outputs:
    - name: Register
      label: "Register's Public Address"
      comments:

fields:
  - name: Header
    label: Header
    description: "Common header data for all actions"
    type: Header
    notes: "Common header data for all actions."

  - name: TextEncoding
    label: Text Encoding
    description: " 0 = ASCII, 1 = UTF-8, 2 = UTF-16, 3 = Unicode.  Encoding applies to all 'text' data types. All 'string' types will always be encoded with ASCII.  Where string is selected, all fields will be ASCII."
    type: uint8
    size: 1
    example_hex: 00
    example_value: "0"
    notes:

  - name: Message
    label: Message
    description: "Length only limited by Bitcoin protocol."
    type: nvarchar32
    example_hex: 0000000000000008757365726e616d65
    example_value: "username"
    notes: ""
