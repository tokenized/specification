#
# Alteration Action: A register entry/record can be altered.
#

code: R3

metadata:
  name: Alteration
  label: Alteration Action
  description: "Alteration Action -  A register entry/record can be altered."

rules:
  fee:
  inputs:
    - name: Register
      label: "Register's Public Address"
      comments:

  outputs:
    - name: Register
      label: "Register's Public Address"
      comments:

fields:
  - name: Header
    label: Header
    description: "Common header data for all actions"
    type: Header
    notes: "Common header data for all actions."

  - name: TextEncoding
    label: Text Encoding
    description: " 0 = ASCII, 1 = UTF-8, 2 = UTF-16, 3 = Unicode.  Encoding applies to all 'text' data types. All 'string' types will always be encoded with ASCII.  Where string is selected, all fields will be ASCII."
    type: uint8
    size: 1
    example_hex: 00
    example_value: "0"
    notes:

  - name: Message
    label: Message
    description: "Length only limited by the Bitcoin protocol."
    type: nvarchar64
    example_hex: 001c4368616e67656420436f756e747279206f66205265736964656e6365
    example_value: "Changed Country of Residence"
    notes: ""
